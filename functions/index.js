const express = require("express"); const axios = require("axios"); const app = express(); app.use(express.json()); const token = "8300141763:AAGDOU2I31OtHUD0d-Sb5_eFGBIC3DmD-K0"; app.post("/apiTelegram", async (req, res) => { if (req.body.message) { const chatId = req.body.message.chat.id; await axios.post(`https://api.telegram.org/bot${token}/sendMessage`, { chat_id: chatId, text: "¡ESTOY VIVO! Aki Online vía Túnel Local. Director Marcus, el equipo está operativo saltando el bloqueo de Google." }); } res.send("OK"); }); app.listen(3000, () => console.log("Servidor en puerto 3000"));
